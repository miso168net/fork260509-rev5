# BACKLOG-DEFERRED — 滯後卷

條目格式同主檔 `BACKLOG.md`；本卷收 user 拍板滯後的待辦——不排入 NOTES 近期 roadmap、STATE
分開計數（**滯後≠完成**、lint 一律視為仍開放）。配號永遠只在主檔（本卷無 next-id）；移入／移回
＝整行搬＋滯後戳記；完成＝照舊刪列＋事件 `backlog_done`；各條目觸發欄寫回收時點。

- B-033｜樣板回灌帳（§3.2 條 16／Q14）：集中登記 docs-governance-template 的樣板缺陷與搬遷摩擦，rev5 收刀後一次批次回灌。已知內容＝①樣板 export 腳本未落致 rev5 手工搬運、樣板失去第一個真實檢驗機會 ②樣板 H 層把 Lint20 具名豁免表當既有設施而源碼實無（rev5 補建）③治理硬化四項顯式延後之邊界清單（lint 差分分級／ctags 符號漂移偵測／基線 freshness 提示／clean-tag 建置閘）應記其存在、防第三代重新發明（詳＝啟動書 §5.2 K2-18／K2-19）④2026-08-08 補登——提前批＋B12 前維護批產出、樣板皆無：bash→python 選擇性轉換判準與機器等價驗收（ADR 0010）、工具版本三層釘定政策（ADR 0011）、跨代 ID 命名空間紀律含具名豁免形（ADR 0012＋Lint25）、互動機密工具 pty 併流與自動代餵安全姿態（ADR 0013）、效能預算節形制（量測法＋單跑上限＋門檻數字單一權威＝RUNBOOK §12.1）、「紅訊息附去處」須連帶驗證出口可執行（B-042 開帳教訓：照去處做仍清不掉紅＝去處失效）、通用工具坑 L-001～L-007 隨批沉積、守門機制非 vacuous 生效自證紀律（ADR 0024）與判準結構性無感即恆綠教訓（L-010；002-system-settings 收刀後新立）｜rev5 收刀後批次回灌｜★滯後 2026-08-25 user 拍板：觸發點＝rev5 整代收官**之後**，定義上不在「近期」——觸發權不在近期 roadmap 內
- B-034｜`alert_webhook_url` 填真值＋回寫密文（現值＝公開佔位字面 `https://CHANGE-ME.invalid/…`、經 ADR 0003 列入 secret-value-guard 白名單故不擋 commit；填真值後自動納回值比對；連動＝`deploy/secrets.dev.enc.yaml` 重加密、RUNBOOK §15 機密營運流程）｜起 observability 軌前（grafana 告警投遞實測時）｜★滯後 2026-08-06 user 拍板：創世收官時**直接立於滯後卷、未曾入主檔**——真值需人工提供且 obs 軌未排程，觸發權不在近期 roadmap 內
- B-043｜envelope 的 5000 回退路徑不覆寫 HTTP status：`Res<T>::into_response` 在 serde 序列化失敗時只換 body（5000 三欄信封）、status 取預設 200，但外層若為元組形 `(StatusCode::X, Res::ok(payload))`，axum 會以元組的 status 覆寫，於是出現「status≠200 卻帶 5000 信封」的自相矛盾回應（002-system-settings 單元 U2b 之 spec-review）。B12 不可達——走元組形的只有錯誤路徑、一律 `Res<()>` 而其序列化無失敗面。★2026-08-09 實證推翻原列兩個處置候選（維護批 maint-b043）：①「回退路徑顯式帶 StatusCode::OK」**型別上不可能生效**——axum-core 0.5.6 之 `impl IntoResponse for (StatusCode, R)` body 為 `*res.status_mut() = self.0;`（response/into_response.rs），無條件覆寫內層產出的 status；②「型別層禁止該組合」**在 Rust 表達不出來**——無反向 impl，只要 `Res<T>: IntoResponse` 成立、任何 handler 都寫得出該元組。真修法＝令回退路徑於**建構期**即不存在（`Res::ok` 改預序列化、失敗即回 AppError 走錯誤路徑），需動 contract-bearing 型別與其釘死測試＋compile_fail doctest，故續留原觸發刀。★同批已落兩件：特性測試釘現況（元組 201＋5000 信封並存，修好那天應反轉為斷言 200）＋不可達前提之機器守（跨行掃生產區，帶 payload 的成功信封不得與 status 元組同表達式；錯誤路徑 Res::from_err 形放行）——即「B12 不可達」自此有機器擋著、不再只是散文｜首個需要非 200 成功回應的刀｜★滯後 2026-08-25 user 拍板：roadmap 無「需要非 200 成功回應」之刀；★移卷之失憶風險由機器守兜底——`envelope.rs` 特性測逐字指名本條（「請連同 B-043 條目一併復核」），真觸發時循該紅訊息即找回
- B-059｜`throttle` 的 `us4_sliding_window_resets_on_success` 有**間歇性假紅**未定位：U-L 期間 85 輪累計 1 次紅（首見於 implementer 的第 1 輪、症狀＝該得 2222 卻得 1000＝L2 count 讀低），此後 65 輪＋主線 20 輪皆零重現。兩個嫌疑向量（dev 共用庫的外部登入流量、L1 GET 瞬斷）查無現行證據。★診斷面已烤進該測（逐敗落列斷言＋失敗時 rows 全列傾印＋redis_lock 降級 delta），再現時可自證屬「外部干擾列」或「redis 瞬斷」哪一形。★2026-08-11 U-N 全量閘**複跑** 14 輪（review 側 7＋修補後 7、皆 serial 全量）零重現——依本條原觸發器**降級為已知態**，僅存檔備查、不再排診斷工。★本條原寫「U-N 全量閘（321 支全綠 rc=0）零重現」係**單輪觀測**、當時並不成立：同期 7 輪中 2 輪紅，紅點屬另一因（redis TTL 上界零餘裕撞牆鐘後跳，見 L-026／B-066）、與本條無關；降級判斷本身不受影響（那 14 輪 `us4_sliding_window_resets_on_success` 確實零重現），改寫的只是佐證句｜再現時據烤入之傾印定案｜★滯後 2026-08-25 user 拍板：正文已載「降級為已知態、僅存檔備查、不再排診斷工」——我方明示不排工，移卷即帳實相符；再現時循烤入之傾印定案
- B-070｜`tools/wf-watchdog.py` 推導上限的 **resume 形缺口**＝已知態（B-069 修法之誠實限縮）：resume 沿用原 runId，持久 json 直到 resumed run 結束前仍是**前一輪**的 script 快照——resume 前若動過 `AGENT_FUSE`（CLAUDE.md §2「修 script→resume」即典型動機），推得上限即為舊值（調高＝偏窄假警報、調低＝偏寬弱化）。行為面無便宜修法：以 json mtime 區辨新舊快照，會把「鎖到已完成 run」形一併打回 floor＝B-069 原始誤報復活（完整論證在該工具常數區註解）；真修需框架端提供 run 級 script 識別｜框架出現 run 級 script 識別面時，或 resume 形實暴誤導一次時｜★滯後 2026-08-25 user 拍板：觸發權在**框架端**（run 級 script 識別面）、我方無可做之事——同 B-034 之「觸發權不在近期 roadmap 內」最典型形
- B-076｜門鈴 `cache::publish` 的回傳值（**訂閱者數**）在生產面**零消費者**：`ipgate::reload_and_publish` 只接 `Err`、把 `Ok(n)` 整個丟棄，而該原語的 doc 逐字寫著「回傳值刻意不吞成 `()`……吞掉即永遠問不出來」——生產面實際上就是沒有任何人問。★**單副本下無害且刻意**（ADR 0014 決策 4 拍板單副本 ⇒ 健康態恆有本行程 watcher 這一個訂閱者、回 `Ok(1)`；`Ok(0)` 只出現在 boot 到訂閱成功、退避窗、訂閱失敗／逾時三種狀態，而這三種watcher 自己都已各發過一則 `doorbell` 降級，在此再發即與真實故障次數對不起來；data-model §5 亦無「零訂閱者」列）。★**觸發後的危害**：若日後拍板多副本，`Ok(0)` 的語意就從「本機還沒訂上」變成**「全部副本的 watcher 都失聯」**——那是規則改了但**整個叢集**都不生效的形，而現行**零訊號**。屆時須連同「`publish` 回 0 是否計降級」「是否需要訂閱者數的下界告警」一併重審。（004 U-G 規格確認輪列為誠實界線、明文建議落帳）。★**2026-08-16 收刀前複驗（結論不變、補一項事實變化）**：立條時 `reload_and_publish` 尚無生產面呼叫者，**U-H 之後有了**——`handler/ip_rule.rs` 的 `reload_after_write` 於四個寫端 commit 之後呼叫它 ⇒ 每次規則寫入都真的按一次門鈴。逐項複核：①「只接 `Err`、丟棄 `Ok(n)`」**仍為真**（該處是 `if let Err(e) = crate::cache::publish(...)`）②「單副本下無害且刻意」**仍成立**（健康態恆 `Ok(1)`）③觸發器（拍板多副本）**仍是對的等待點**。★唯一該記下的差別＝`Ok(0)` 自此是**生產面真的到得了**的狀態（寫入落在 watcher 退避窗內即是），只是單副本下它與該窗自己已發的 `doorbell` 降級重複、故仍不另計｜拍板多副本部署時，或 ADR 0014 的單副本決策被翻案時｜★滯後 2026-08-25 user 拍板：觸發權＝翻案 ADR 0014 之單副本決策，而 prod 不入 roadmap（同 ADR）——觸發權不在近期 roadmap 內
- B-077｜IP 閘阻擋告警**無節奏 gate**：`middleware::ip_gate_mw` 的 deny 腿每次阻擋發一則 `warn`（帶命中網段／真實來源／路徑），零時間窗、零 per-cidr 抑制、零取樣；rev4 同一則以 redis `SET NX EX 60` 壓成 **≤1 warn/60s/cidr**（rev4:`BLOCKED_WARN_PERIOD_SECS`），rev5 隨 `blocked_breadcrumb` 不搬（出處＝research **R1 第 17 列**「`blocked_breadcrumb` 觀測輔助段另在 221–278、本刀依 FR-018 需其等價物」；★**不是** R2-8／R2-9——那兩列分別是觀測層 HLL 兩支與 `access_log_mw`，皆未談 breadcrumb）把承載機制拿掉時，**一併拿掉了「量的上界」這條需求**而未另立替代——★注意 R1-17 逐字要的是「其**等價物**」，而現行只等價了量級訊號、沒等價頻率上界。★危害面：IP 閘跑在身分驗證**之前**（`router.rs` 組裝序，由 `wired_router_ip_gate_runs_before_identity_enforcement` 釘住），故該腿**無需任何憑證**即可觸發、也不受來源維節流（T047）約束——被擋方能以請求速率決定我方 log 量與 stdout 寫入鎖爭用（tracing-subscriber json 形同步寫、共用一把鎖），屬自我放大形；本應是全鏈最便宜的一種請求（零 I/O、最外層短路）反而成為唯一造成鎖爭用的一種。★**當下不修的理由**：FR-018 **逐字**要求「每次阻擋 MUST 留下帶結構化欄位的可觀測紀錄（**含命中的網段**）」，而 `ipgate_blocked_total` **刻意無 label**（`obs::IPGATE_BLOCKED_TOTAL` doc：規則列數由營運面決定、無界，網段做 label＝把序列基數交給外部輸入且 recorder 無淘汰機制）⇒ 命中網段這一欄**只存在於 warn 裡**，加節奏 gate ＝被抑制的阻擋完全不剩該欄 ＝改 FR-018 字面，屬拍板級。★**嚴重度界定**：`sys_ip_rule` 出貨時零列 ⇒ deny 腿當下**永不觸發**、warn 量恆零；要等營運面加了 deny 規則且該來源持續打才長得出來。★三個處置選項（擇一需拍板）：①改 FR-018 字面允許節奏壓制 ②給計數器補**有界** label（如 `rule_id`，不用網段字面）後再壓 warn ③接受現況、僅在 runbook 記下「加 deny 規則後 log 量由被擋方決定」。（004 U-G 碼品質審查提出，fix agent 以 FR-018 字面為據拒改並升級主線；碼註已誠實化＝`middleware/mod.rs` 檔頭逐字記下「等價的是量級訊號、告警頻率不等價」）｜下一把動 FR-018 字面或 IP 閘觀測面的刀，或營運面回報阻擋 log 量過大時｜★滯後 2026-08-25 user 拍板：roadmap 無「動 FR-018 字面或 IP 閘觀測面」之刀，且 `sys_ip_rule` 出貨零列 ⇒ deny 腿永不觸發、warn 恆零；真觸發＝營運面加 deny 規則後由人回報，不倚賴本卷提醒
- B-053｜`/auth/error` demo 端點於 rust-api 兌現——base-web fork 原版兩張 demo 頁（`views/function/request/index.vue`／`views/alova/request/index.vue`）經 `fetchCustomBackendError(code, msg)` 呼叫 `GET /auth/error`，003-auth-session 之 16 條 ROUTES 不含此端點、按鈕點擊得 4040＋`system.notFound` 屬已知態（依憲法 §I.1「v1 從簡只能是交付排程」走排程錨、非設計範圍縮減）。★前置衝突（非工期）：該端點契約＝回吐 client 任意 `code` 與 `msg`，而 demo 字面含保留碼 `9999`（「後端從不發出」由 `error.rs` 兩處 `no_variant` 陣列＋`envelope.rs` 之 `compile_fail` doctest 三錨釘死；`Res` 三欄非 pub、合法出口僅 `Res::ok`／`Res::from_err`）、`msg` 又是 `$t()` 已在地化人話（違 §I.3「msg 載穩定 i18n key」、並破 msg-dict 之後端實發 13＋前端白名單 9＝22 算術）⇒ 兌現須先走 §I.3 Amendment 決「保留碼是否開特例」與「msg 是否開 echo 通道」，兩者皆屬 13 碼矩陣凍結面｜demo／B-018 前端刀，或下一把觸及 wire 例外集的刀★2026-08-25 滯後（007 brainstorm Q40、user 拍板）：前置＝§I.3 NON-NEGOTIABLE Amendment（保留碼 9999 特例＋msg echo 通道），刀 B 不觸；回收時點＝下一把觸及 §I.3 wire 例外集的刀。
