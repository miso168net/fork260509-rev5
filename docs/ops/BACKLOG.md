<!-- next: B-139 -->
# BACKLOG — 待辦

條目格式 `- B-NNN｜<一句話>｜<觸發條件或期限（選）>`；配號取檔頭 next-id 後 bump、號碼永不回收；完成即刪列、git 即史。
滯後項另居 `BACKLOG-DEFERRED.md`（user 拍板暫不排程者；★滯後≠完成——lint 視為仍開放、STATE 分開計數）——查待辦全帳須兩卷併看。

- B-078｜**「稽核欄含 `real_ip`」的字串包含式複驗，在 IPv6 非正規化字面下假陰性**：`trust::rightmost_window_str` **逐字保留**鏈欄原文（刻意——不可解析欄是鑑識痕跡），而 `real_ip` 是 `to_canonical(..).to_string()` 的**壓縮小寫**形 ⇒ 鏈裡寫 `2001:0DB8:0000:0000:0000:0000:0000:0009` 或 `2001:DB8::9` 時，`real_ip` 落 `2001:db8::9`，`x_forwarded_for LIKE '%'||real_ip||'%'` 得 `f`。★**方向是假陰性**（該跳其實**仍在窗內**、以同一份取窗＋正規化重推得同一結論），故憲法 F8 的條文本身**不失準**——F8 講的複驗是「重推」不是「字串包含」。★**危害觸發點**＝有人把 quickstart §1b 的 `LIKE` 判別搬進正式稽核報表、對賬腳本或機器閘：屆時合法的 IPv6 來源會被系統性報成「稽核欄不含真實來源」＝憑空製造一批假的鑑識缺口告警。★**不受影響的形**：帶埠 `1.2.3.4:5678`、帶區域 `fe80::5%wlan0`、方括號包裹者，正規化後仍為原字串的子字串。★候選處置：①複驗一律走「重推」路徑（讀鏈欄→`normalize_xff`→`resolve_client_ip`→比位址值），別用字串包含 ②若非要字串比對，先把鏈欄逐欄正規化再比（但那會丟掉逐字痕跡、與 `rightmost_window_str` 的刻意設計相抵）。（004 U-M 規格確認輪列為 notes 級、明文建議落帳）｜有人要把稽核欄複驗寫進報表／機器閘時，或稽核管理頁刀
- B-082｜**來源維節流計數的 `real_ip` 側無索引可用**：`count_recent_failures_by_ip` 的 `real_ip <<= $1::inet` **吃不到任何 btree 索引**（btree inet 運算子族不含 `<<=`；004 U-J 實測執行計畫把該述詞放進 **Filter**），現況完全靠 `created_at > GREATEST(...)` 這個述詞定界。★**是容量議題、非正確性議題**（FR-026 不受影響、判定恆正確）：`sys_login_attempt` 為 append-only 且隨營運持續成長，屆時每次來源維 precheck 都要在**窗內全部列**上做 Filter。★候選處置＝建 `CREATE INDEX … USING gist (real_ip inet_ops)`（或 spgist）——★**屬 schema 變更、須走 migration＋schema-gate 三閘**，遠超當時單元邊界故未做。★觸發條件＝窗內列數量級明顯上升，或 precheck 的 p99 出現退化；**非立即工項**。出處＝`model/facade/sys_login_attempt.rs` 之 `count_recent_failures_by_ip` rustdoc（機制與出路已就地記載）。｜稽核表成長到窗內 Filter 成本浮上來時，或下一把動 `sys_login_attempt` 索引面的刀
- B-001｜承襲盤點機器閘＝**暫不建**（002-system-settings 收刀留帳、FR-028）：要求①的 K1 盤點表見該刀 brainstorm §2、要求②的實際消費對照表已於 plan research R10 回填。R10 結論＝五條 K1 全數消費、其中四條有機器強制錨（K1-07 覆蓋閘＋gen.router 真表／K1-25 wire-schema drift 閘＋coverage gate／K1-26 registry 紅綠矩陣／K1-27 contract case 與授權矩陣），K1-08 屬一次性選型無殘留紀律故無需錨——「靠人記得」面＝零，建閘的邊際收益低。★復議觸發條件：某刀出現「K1 條目被消費卻無任何機器錨」的情形，或盤點表與實際消費連續兩刀對不上｜觸發時重評
- B-003｜memo 欄家族 UI 兌現——**餘 sys_user／sys_role／sys_menu 三張**管理列表的顯示欄＋編輯入口（語意：R_SUPER 備註用途、text 可多行；顯示於管理列表、不顯示於其它被取用處——下拉／引用／對外 API 不帶；rev4 設計入 schema 但各 UI 刀 brainstorm 均未兌現，語意權威＝001 刀 data-model）。★**2026-08-16 四分之一關帳**（004-ip-trust-anchor U-I／T039~T043）：`sys_ip_rule.wbip_memo` 已兌現且**兩面俱全**——列表顯示欄（`views/manage/ip-rule/index.vue` 之 `wbipMemo` 欄）＋抽屜編輯入口（`modules/ip-rule-operate-drawer.vue` 的 textarea）。★連帶落下一個**可抄的範本**：該頁的自由文字欄一律純文字插值（JSX `{row.wbipMemo}` 由 Vue 逸出），並有機器守 `tools/view-render-guard.py` 掃 `views/manage/**` 零原始 HTML 插值 ⇒ 餘三張頁兌現時照抄即滿足 B-072 的渲染端義務。★2026-08-22 四分之三關帳（005-role-menu-crud）：`sys_role.role_memo`（前端 role 頁單元：列表「角色备注」欄＋drawer textarea）與 `sys_menu.menu_memo`（前端 menu 頁單元：樹表「菜单备注」欄＋modal textarea）皆兩面俱全兌現、placeholder 標「仅管理员可见」、渲染照 ip-rule 純文字插值範本（view-render-guard 覆蓋、CDP 走查實證）。**餘 sys_user 一張→刀 B**｜刀 B（user＋password）brainstorm 直接輸入
- B-008｜**餘兩張** rev4 專屬管理頁 view 於 base-web 兌現——manage_system-settings／manage_audit（★`manage_policy-archive` 已於 2026-08-23 由 006-authz-governance **出列**：頁本體＋search 模組＋回收桶兩支端點〔U6／U7a〕，seed 列 10 自此有 view、`tools/seed-view-gate.py` 豁免表只剩本條餘兩張；`manage_ip-rule` 已於 2026-08-16 由 004-ip-trust-anchor **出列**：頁本體＋回收桶＋防自鎖體驗全套落地〔U-I／T039~T043〕，其 5 支後端端點亦隨 U-H／T031~T036 上線；連帶 ⇒ 下方「12 支不存在的端點」實得 **5 支**＝audit 5（policy-archive 2 支已隨 006-authz-governance 上線），且 §III.2 第五條 ★ 軌道 `BASE-WEB-MANAGE-PAGE-WIRING` 已開、餘兩張頁走「在既有名冊上加用途」）（analyze D6：seed 選單與 casbin 政策隨 001 基線先行、`component` 指向之 view 於 rev5 base-web 尚不存在；期間該 4 項僅 R_SUPER 可見、點擊 404 屬已知態）。★2026-08-11 更新（003-auth-session 落地後，2026-08-09 那則勘誤的兩處前提皆已反轉）：①`base-web/.env` 已由 U-I／T028 翻為 `VITE_AUTH_ROUTE_MODE=dynamic` ⇒ 「僅 R_SUPER 可見、點擊 404」**自此成立**，不再是待兌現的前提；②「該刀第一件事＝憲法 §III.2 **首個** ★軌道 Amendment」亦已不成立——ADR 0028 已於 003 開立四條 ★ 軌道八用途，本條後續刀要做的是**在既有名冊上加用途／加軌道**，不是開第一個。另二實測：①`elegant/routes.ts` 的 manage 子樹只有 menu／role／user／user-detail，**連 route 條目本身都不存在**（`component` 指向 view.manage_* 那句的來源在後端 seed、不在前端）②新增任一 view 必然 inline 動 zh-cn.ts／en-us.ts（route 鍵為窮舉型 Record）與 app.d.ts（面級新增），另三張頁原需 12 支不存在的後端端點（audit 5／ip-rule 5／policy-archive 2）——★ip-rule 那 5 支與 policy-archive 那 2 支已上線，**餘 5 支**。★**2026-08-17 實測更正（004 收刀後 CDP 走查）——「點擊 404」這句只對一條路徑成立**：①**直接打網址**（`/manage/audit` 等）→ 確實是 404 頁，本條與 ADR 0018 記的已知態成立；②**從側欄點該項** → **零反應**：URL 不變、無 404 頁、無 console error、無 toast（前端 router 解析不到該 `component`，導航被靜默中止）；③側欄顯示的是**未翻譯的原始 i18n key**（`route.manage_system-settings` 等字面直接上畫面）——因為 `route:` 樹是窮舉型 `Record<I18nRouteKey, string>`、這三個鍵在兩語 locale 皆不存在。★**這個更正有實害、不只是措辭**：ADR 0018 後果段要求「前端煙測腳本須把該 4 項列為已知態、防誤判回歸」，而煙測若以「點擊得到 404」為判準，**它驗的是使用者根本走不到的那條路徑** ⇒ 該守本身失準。真實使用者路徑（點側欄）的正確判準是「零反應＋顯示原始 key」。★ADR 0018 body 不可變，**as-built 事實以本條為準**（同憲法 v1.6.1 對 ADR 0043 的處置形）。｜各對應管理 UI 刀 brainstorm 直接輸入
- B-016｜稽核資料生命週期一次入設計：retention 水平線語意＋清理動作同交易自記稽核＋自動排程＋逐表門檻，不走「先手動清理、後補自動化」（前代同域四次拍板／一次正式翻案／一次違憲重拍；rev5 稽核表結構已隨 001 基線壓平落地、政策面尚未設計；詳＝啟動書 §5.2 K2-01、承 rev4:ADR 0058／rev4:0075／rev4:0076＋specs/rev4:017-audit-retention）｜稽核功能刀★2026-08-24：006-authz-governance 令 sys_casbin_policy_archive 自此有消費面（restore 即刪列）——歸檔表的 retention／清理政策屬本條射程、設計時一併納入。
- B-018｜前端 demo 資產去留一次拍：alova 第二請求棧、替代登入表單殼（seed 側 demo 選單與授權已由憲法 §I.2＋ADR 0005 收；前代全量保留衍生兩支 won't-fix 決策＋清理需 seed 移除白名單；詳＝啟動書 §5.2 K2-03、承 rev4:ADR 0029／rev4:0035／rev4:0036）｜★觸發器 2026-08-12 重定（原「前端首刀」＝003 已過站未消費）：003 之 U-M 把三張替代登入表單誠實化、B-029 更新段又把 `views/alova/scenes/modules/captcha-verification.vue` 指回本條 ⇒ alova 第二請求棧與 demo 資產是**最後一塊沒人認領的前端 demo 面**。改為：下一把動 base-web demo 面的刀，或 prod 硬化前（B-064 同批）★2026-08-24：006-authz-governance 把 role 抽屜三顆 modal 假資料面全數消滅（接真）；demo 殼殘餘＝system-manage.ts 之 user／menu demo fetcher 與 alova 棧，去留拍板面不變。★2026-08-25（007 brainstorm Q39）：`system-manage.ts` 之 `fetchGetUserList` demo fetcher 自刀 B 起亦失消費者（user 頁改打 `rev5-user-admin.ts`）；觸發器改狀態式——「轉 prod 時（與 B-064 同批）或下一把動 base-web demo 面的刀」（ADR 0014 prod 不入 rev5 roadmap、「prod 硬化刀」非實體刀）。
- B-020｜失敗計數節流做成可掛任意敏感端點的通用 seam（per-user／per-IP），不綁死登入流程（前代改密端點無法直掛成獨立缺口、軟區永不被第一層快取短路；輕量範本＝原子先佔 SET NX→INCR→超限拒→best-effort 回補；詳＝啟動書 §5.2 K2-05、承 rev4:ADR 0037／rev4:0038→rev4:0045）｜★觸發器 2026-08-12 重定（原「節流刀」＝003-auth-session 之節流三區已交付、過站未消費）：003 只做 per-user 維且**老實記為 login 專用實作、不宣稱通用**。兩個半邊各有歸宿——**per-IP 半邊**＝IP／信任錨刀（依賴其產出的可信來源 IP）；**通用化半邊**＝待第二消費者存在（＝B-021 的改密端點，rev5 尚未建）。★**2026-08-16 半邊關帳**（004-ip-trust-anchor U-J／T044~T049）：**per-IP 半邊已交付**——來源維節流上線（計數桶＝`real_ip` 經 `throttle::ip_bucket` 導出、v4 `/32`／v6 聚合 `/64`；門檻三鍵 `ip_captcha_after`／`ip_max_fails`／`ip_window_minutes`；與帳號維並列判定後合成「任一硬鎖→硬鎖；否則任一軟區→軟區」）。★**本條殘餘射程自此只剩「通用化」一件**，且**觸發條件不變**：rev5 現有的兩個維度仍都住在 login 流程內、`throttle::precheck` 仍是 login 專用簽章；第二個消費者（B-021 的改密端點）依舊不存在 ⇒ 現在通用化仍等於為單一消費者設計抽象★**2026-08-27 刀 B U0 複核（刀 B 期間狀態）**：第二消費者自本刀起**存在**（`throttle/change_pwd.rs` 改密舊密節流），但採**專用桶**落地（鍵前綴 `cpwd:` 與登入節流鍵面分離、門檻碼內常數、fail-open 進 `throttle_degraded_total` 新 source `redis_change_pwd`）、**刻意不通用化** `throttle::precheck`——兩消費者的判定維度（登入＝帳號維＋來源維雙軌合成／改密＝per-user 單軌）、軟區語意（登入有 captcha 軟區／改密無）、觀測 source 皆不同，現在抽共用 seam 等於為兩個形狀不同的消費者硬造抽象。★本條殘餘射程與觸發器不變；**是否通用化之定案排刀 B 收刀（T073）**。
- B-021｜改密端點舊密暴力試節流（掛點在 argon2 之前、成功改密清計數、需新業務碼；殘餘拍板點＝redis 故障 fail-open 或 fail-closed、門檻走設定鍵或常數；詳＝啟動書 §5.2 K2-08、承 rev4:B-102＋specs/rev4:014-user-center／rev4:015-pwd-custody）｜★觸發器 2026-08-12 重定（原「auth 設計期內建」＝003 已收、改密端點未建）：本條唯一前置**不是節流技術而是「改密端點得先存在」**——rev5 之 16 條 ROUTES 內零改密端點、`password_*` 八鍵零消費者。改為：改密端點建立時（user 中心／使用者管理刀）
- B-023｜備份自動化第二段：排程化（cron 或 compose sidecar）＋機密檔與資料卷的配對備份＋還原演練自動化（第一段已收單＝deploy/backup-db.py＋非破壞 scratch 演練＋RUNBOOK §6、維護批 merge 4e97031；詳＝啟動書 §5.2 K2-11、承 rev4:B-107）。★軌別＝**輕量軌**（2026-08-09 定，同第一段）：第一段那支維護批新增 `deploy/backup-db.py` 427 行全新工具、同批另進 `tools/wf-watchdog.py` 512 行——可見「不新增能力面」在本 repo 的實際判讀是「不新增**產品**能力面」而非「不新增工具」，第二段照此軌走、毋須另開 SDD｜B12 後治理批（prod 不入 roadmap＝ADR 0014、免異地／加密升級要求）
- B-024｜寫端授權下放非超管前置三件套：no-escalation 上限檢查、seeded 受保護護欄與「超管恆禁停用」結構護欄複評、業務錯誤明細通道受眾邊界重評（詳＝啟動書 §5.2 K2-12、承 rev4:B-083＋specs/rev4:009-role-admin）。★2026-08-09 勘查補記（ADR 0022 已 accepted、body 不可變，故三筆記於此）：①該 ADR 第 3 款稱「B-024 接手時零簽章變更、零呼叫端改動」**在真邏輯需要請求 body 時不成立**——`no_escalation_check` 四參數為 db／actor_uid／path／method、無 body 通道，而「不得授予超出自身權限之角色」的資訊住在 body，且 `require_policy` 是 middleware、跑在 body 解析之前 ②隱藏前置＝政策熱重載**已由 005 T009（ADR 0049）兌現**——`enforce.rs` 之 rebuild-swap `reload_enforcer` 落地、「boot 載入即終態」原拍板同批翻案；授權治理刀 grant 面屆時純消費同一支、零新機制（本②自此非前置） ③該 ADR 後果末條交棒之「填入查庫邏輯後把掛點呼叫移到取讀鎖之前」無任何測試會自動抓它沒做，tasks 須顯式列一條。★另註落地對象在 rev5 後端**仍為零實作**（2026-08-12 更正：原括號「handler 目錄僅 system_settings.rs、全服務寫端 route 僅 1 條」已被 003 推翻——現有 auth/ 五支＋captcha＋route＋system_settings、ROUTES 16 條；**無 role／user handler** 這個結論不變），本條只能是 role／user 管理刀的一章、不能單獨成刀｜role／user 管理刀（目標含多層管理員即入首版）★2026-08-24（006-authz-governance 收刀）：三件套之「seeded 受保護護欄」擴充面已由該刀落地（ADR 0054 結構性封死＋protectedRevoke／protectedGrant 整批拒）；本條殘餘＝①no-escalation 真掛點（含 body 通道課題）③明細通道受眾邊界——仍待授權下放刀。
- B-025｜軟刪×授權歸檔一致性通用掃描（選單／角色／使用者各軟刪路徑下 casbin 碼與選單按鈕欄聯集），宜與背景 job 底座同刀首發（詳＝啟動書 §5.2 K2-13、承 rev4:B-100）。★2026-08-22 敘述更新（005-role-menu-crud 收刀）：role／menu 兩軟刪路徑的**寫入時點**一致性窗已被本刀消滅——歸檔掃描前置於軟刪 UPDATE＋同交易＋advisory 序列化域（rev4「先軟刪後歸檔」窗客戶不復存在）；本條殘餘價值收窄為①sys_user 軟刪路徑（刀 B 落地時同窗重評）②**事後對賬**掃描（防缺陷／手改 DB 造成的既存漂移——寫入面再正確也擋不住存量壞資料）｜背景 job 底座刀★2026-08-24：006-authz-governance 三維 revoke＝同交易 archive-move、射程＝候選集（ADR 0056）——未新增寫入時點窗；事後對賬掃描之殘餘價值不變（掃描面自此含端點維歸檔列）。
- B-027｜列表欄位排序能力＋排序欄白名單三端單一來源（或 parity 檢查）＋per-column 索引評估（前代全數後端預設排序、白名單分散三端靠人工同步；詳＝啟動書 §5.2 K2-15、承 rev4:B-036）｜首個排序需求刀
- B-028｜sea-orm additive DDL 草稿生成輔助（entity 漂移閘已隨治理套件搬運兌現；詳＝啟動書 §5.2 K2-17、承 rev4:B-109／rev4:B-110）。★量測半條已於 002-system-settings 收單（FR-027）：兩輪容器內 cargo build 基線落帳 RUNBOOK §12.2——冷編 43.9s→46.6s、單檔增量 2.26s→3.35s（server 依賴進場前後）；該數據即 dev profile debuginfo 裁剪與否的評估前提｜需要 additive DDL 的刀
- B-029｜captcha 強化包：「答對但登入失敗即自動換題」收進首版前端行為；產圖對抗性（干擾強度、字型多樣）續留觸發制（詳＝啟動書 §5.2 K2-21、承 rev4:B-075）。★★2026-08-11 現況更新（003-auth-session U-L／U-M 落地後，原「前後端皆零」與「整套首版」之描述已失真）：**圖形驗證碼整套首版已完成**——後端 `captcha/mod.rs`＋`handler/captcha.rs`＋`GET /auth/loginCaptcha`，前端 `pwd-login.vue` 軟區條件渲染；`hooks/business/captcha.ts` 亦已由 U-M／T064 改打 `/auth/sendCaptcha` stub、500ms 假延遲與假成功 toast 皆已移除（不再屬 B-022 假成功家族，但它仍是**簡訊**驗證碼倒數殼、非圖形驗證碼）。`views/alova/scenes/modules/captcha-verification.vue` 仍是 alova demo（屬 B-018 去留對象）。★**2026-08-25 帳面更正：原①「答對但登入失敗即自動換題」已落地、自本條出列**——`base-web/src/views/_builtin/login/modules/pwd-login.vue` 之 `handleSubmit` 於「軟區已開啟時的其他失敗」一律 `await refreshCaptcha()`（主案＝答對 captcha 但密碼錯 1000；碼註逐字載 003 T057 DoD 與 quickstart §4 的 2→3→4→5 推進理由），係 003-auth-session 落地時即已兌現、本條當時漏跟。★本條殘餘射程自此**只剩**產圖對抗性（干擾強度、字型多樣；現行 Noise 0.4＋Wave 2.0/20.0、34 字集受內嵌字型 glyph 所限）｜★觸發器 2026-08-12 重定（原「圖形 captcha 首版刀」＝003 U-L／U-M 已完成，正文已更新但觸發欄漏跟 ⇒ 殘餘兩件小項現無任何刀會撿）：改為下一把動登入頁行為或 captcha 產圖的刀
- B-030｜低位殘項群逐項內建 checklist：契約測試對 query 零判別力、zh-cn 鍵集不在掃描面、320px 窄屏頁首溢出、單一超管軟鎖自解的雞蛋相依、告警僅 webhook 單通道、機密清單 parity 檢查（正確強化形＝新增斷言、非單一來源生成——實際四個面且基數各異各有語意：generate 13／preflight 13／compose 12〔reaper 刻意不進〕／secrets.dev.enc.yaml 10〔composite 不入密文檔〕，且 deploy/preflight-secrets.py 明載兩處各寫一份、不得順手合併＝B-037）（詳＝啟動書 §5.2 K2-22）。★2026-08-09 逐項重驗三筆：①「自助頁雙卡同構重複」**在 rev5 不成立、已自清單刪除**——`views/user-center/index.vue` 僅 7 行 `<LookForward />` 空殼（rev4 才有 email／phone 雙卡各約 85 行）②「未刪選單列表分頁」**仍成立**、機制已換為 useNaivePaginatedTable（`views/manage/menu/index.vue:21` hook＋`:253` `:pagination`）③「zh-cn 鍵集不在掃描面」經核**準確、照原文修即可**——`tools/docs-sync.py` 明載「前端側僅 zh-tw；en-us 由 msg-dict 兩語鍵集斷言間接守；zh-cn 不在任何 lint 掃描面」，勿擴大為「en-us 亦不在掃描面」★**2026-08-25 帳面更正：原首項「未刪選單列表分頁」已處置、自本條出列**——與 B-097 係同一事之兩處記帳（本條與 B-097 互不指涉、係本次重分類實測查出），已由 ADR 0060 拍板「凍結治理清單分頁列」落地（UI 位置不動、頁碼 1／每頁 0／整列上鎖），B-097 同批關帳；殘餘的「>100 列不可達」另立 B-131、「回收桶每頁殘留 100」另立 B-132。｜重寫對應模組時逐項
- B-064｜登入頁三顆快速登入鈕暴露 dev seed 帳密（Super／Admin／User＋123456 字面在前端 bundle）——dev 驗收利器、prod 即帳密洩漏，★轉 prod 前必須拆除；拆法屬 §III.2 授權面（動 upstream 既有登入頁）或 build-time 旗標，屆時拍｜prod 硬化刀★2026-08-25 硬約束（007 brainstorm Q41）：**刀 B 期間必須保留**——CDP 三角色驗收（Q10 按鈕 gating 須反覆切 Super／Admin／User）依賴這三顆鈕；觸發器改狀態式「轉 prod 時」（ADR 0014）、拆法屆時再拍（build-time 旗標 vs 直接移除）。
- B-072｜稽核管理列表對 `x_forwarded_for` 的**渲染端轉義**＝未落帳的安全義務：該欄為 client 可控之不可信原文（`request_context.rs` 建構點只做「零 CR/LF＋≤1024 字元」淨化，**不剝 HTML／不做語意驗證**），003 spec 兩處（Edge Cases 與 Out of Scope）皆載「渲染端**必須轉義**、帳面隨稽核 UI 刀」，但該帳從未建立——BACKLOG 與活書 grep 皆零命中，而 003 收刀後 spec 已成史料、不再是查用點。★屬 XSS 級義務、非樣式問題；三張稽核表（`sys_access_log`／`sys_login_attempt`／`sys_operation_log`）皆有此欄。★**2026-08-16 更新（004-ip-trust-anchor；★本刀刻意不宣稱關帳）**：**防線與機器守已備、真實渲染點仍未出現**——①慣例落地＝管理頁自由文字欄一律純文字插值（IP 規則頁的備註欄即首個範例）②機器守＝`tools/view-render-guard.py`（掃 `base-web/src/views/manage/**` 零原始 HTML 插值、7 條禁用字面，掛 pre-commit 條件觸發、其接線另有 `docs-sync.py` 的 `TestGateWiring` 兩支成對案守；spec FR-038）。★**為何不關帳**：`sys_ip_rule` **無** `x_forwarded_for` 欄、本刀新頁**不渲染它**，故本刀交付的是可繼承的防線，不是該欄的渲染。真正的渲染點隨稽核管理頁刀（B-008 之 audit 頁）出現時才對得上帳｜稽核管理列表首次渲染該欄時（B-008 audit 頁）
- B-089｜**前端登入表單的密碼格式規則比後端嚴，且不對稱面無人看管**：`base-web/src/constants/reg.ts` 之 `REG_PWD = /^\w{6,18}$/`（＝`[A-Za-z0-9_]` 6~18 字元）綁在 `formRules.pwd`、由 `pwd-login`／`register`／`reset-pwd` 三支共用；**後端零格式約束**（`sys_user.password` 僅 `String`，login 只驗雜湊）。★**今日不可達**（已實測：3 個帳號、密碼只有一個值 `123456`，合規；`register`／`resetPwd` 是 ADR 0038 的誠實 stub、無真設密碼路徑）——故**現在不改碼**。★**觸發後即為真缺陷**：使用者域寫端進場後，管理員設一個帶特殊字元或 >18 字元的密碼 ⇒ 該使用者**完全無法從 UI 登入**，且症狀極具誤導性——前端在送出前就擋下、**零 API 請求**，錯誤訊息「密码格式不正确」指向使用者自己打錯，而真因是本系統前端自己的規則。★**兩個檔皆為未動過的 upstream 基線碼**（rev5 標記數 0）⇒ 動它＝fork-delta 修改型須帶 `原行:`＋走 §III 授權判定，且屬 **user 可見行為變更＝拍板級**（CLAUDE.md §5），不得順手改。★候選修法三選一（動工時拍板）：①放寬 `REG_PWD` 至與後端一致（等於取消前端這道弱防線）②維持規則但把後端設密碼端點**同步收窄**成同一規則（前後端對稱、代價是密碼強度上限被 `\w` 綁死）③維持現狀但在設密碼 UI 明示規則（成本最低、但「登入被擋」的誤導訊息仍在）。｜使用者域（role／user 管理）寫端那一刀
- B-091｜LESSONS 存量 `promoted_to:` 佔位條逐條盤點回填（分檔遷移已實填 35 條實值、佔位 12 條經 005 收刀順盤 3 條〔L-006／L-014／L-021、2026-08-22〕餘 9 條＝L-001／L-012／L-022／L-023／L-032／L-037／L-038／L-042／L-043）；盤點順便是防法品質審查——寫不出晉升面的條目，往往防法本身不可執行｜下一筆新 LESSONS 條目落檔時順手盤數條，或下一次 review 輪★2026-08-24（006 收刀順盤）：本刀新條 L-054 實填 promoted_to；L-052／L-053 為佔位形（候選晉升位已各自寫明）——佔位 9→11 條；下次 review 輪順盤。
- B-093｜**deleteRole 免 reload 的判定面繼承窗（刀 B 前結構性不可達）**：刪角色→同 code 重建→指派使用者三步後、下次任一移除面 reload 前，新角色於 in-memory 判定面繼承舊實例全部 p 列授權（DB 面乾淨——deleteRole 歸檔已掃盡；殘留僅判定面、任一移除面寫端觸發 reload 或重啟即清）。現況不可達＝user 角色指派寫端不存在（ADR 0050 §3 之 grilling G6 同源；005-role-menu-crud 品質審查輪查定、宣稱已在 sys_role.rs delete 段碼註收斂到 DB live 面實際射程）。★刀 B MUST 於指派寫端落地時復核 ADR 0050 §2 免 reload 論證並擇一閉合：①指派寫端自帶 reload（零既有拍板變更、最可能解）②deleteRole 改觸發同步（推翻 spec 005 FR-013 後半、需拍板）③addRole 唯一守門擴及非活性 code（推翻 FR-009 活性唯一、需拍板）。｜刀 B（user＋password）之角色指派寫端單元★2026-08-24：006-authz-governance 未動 user 指派面、窗形不變；該刀已使三維寫端與 restore 皆 Applied 即 reload（移除面仍 if-archived）——繼承窗僅剩「deleteRole→同 code 重建→指派」序，刀 B 起手必復核本條。
- B-098｜**wire-schema 快照對 Api.RoleAdmin／Api.MenuAdmin 僅被動存放、後端序列化未受裁判**：`server/tests/wire_schema.rs` 現行裁判測未涵蓋兩命名空間（U8 RoleAdmin／U11 MenuAdmin 兩次進場皆同形）——快照被重抽比對面只驗「前端 d.ts 與快照一致」，後端 `MenuRecord`／`RoleRecord` 序列化形漂移不會紅在 wire_schema 面（現行另有 handler 層 key-set 釘死測兼防、非零防線）。候選修法＝比照既有命名空間裁判形擴兩支（005 U11 碼品質審查輪 N7）。★2026-08-23（B-094 收攏批、006 brainstorm §10-21 拍板）：RoleAdmin 6＋MenuAdmin 6 已於 `tests/wire_schema.rs` 補齊裁判（每 definition 正向＋反例各≥1、24 支）；**餘 Api.IpRule.* 七支仍被動存放、留帳不補**（本條不關帳、觸發器不變）｜下一把動 wire-schema 工具面或 contract 測家族的刀★2026-08-24：Api.RoleAdmin 本刀新型（12 支）與 Api.PolicyArchive 四型已隨 006-authz-governance T033 補裁判（wire_schema 71 測、各正向＋反例）；殘餘面恰 `Api.IpRule.*` 七支續留帳。★**2026-08-27 刀 B U0 複核（刀 B 期間狀態）**：本刀新增 `Api.UserAdmin.*`／`Api.UserCenter.*` 兩命名空間並**隨刀補齊裁判**（每 definition 正向≥1／反例≥1、T067），`Api.IpRule.*` 七支**續留帳不補**（spec FR-041 逐字）；本條不關帳、觸發器不變。
- B-107｜**`handler::common::audit_operator` 缺席腿的 log `target="security.ipgate"`＋`degraded` 欄無機器守（改壞全綠、obs016 告警當場失明）**：既有兩支測只斷言 `5000` 與 `AuditOperator` 五欄；`deploy/grafana-provisioning/alerting/rules.yml` 之 ipgate-degraded 以 `target="security.ipgate" | fields_degraded=~".+"` 過濾，把 target 字面改掉或拆 degraded 欄＝告警結構性進不了 count_over_time 而測試照綠。缺口在收攏前四份拷貝時即存在（pre-existing），收攏後爆炸半徑由「四份各自失效」變「單點失效服務四域」（B-094 收攏批 U1 碼品質輪查定）。候選守法＝tracing_subscriber 捕獲層測斷言該則 target 與 degraded 欄、或「source 字面 ↔ rules.yml 過濾字面」名冊閘（與 obs.rs pre-register 同族）｜下一把動 obs／告警名冊或 tracing 測試基建的刀
- B-113｜**刀 B 把 seed 未來端點註冊補齊後，重審 ADR 0056 之 R_SUPER wire 案前提自證**：handler/role.rs endpoint_tests `update_role_endpoints_super_full_candidate_save_applies_0000_and_leaves_out_of_scope_seed_rows` 以三前提自證（R_SUPER 現役含候選外列≥1、候選內列數＝候選集大小、候選內含 protected≥1）釘住射程濾點；刀 B 註冊 updateUserSessionPolicy 等端點後「候選外列≥1」會由綠轉紅——屬測試失去鑑別力的誠實訊號、非缺陷；屆時改以合成候選外探針列（非 seed）重建前提、勿刪測（006-authz-governance U5c 實作期查定）｜刀 B（user＋password）★2026-08-25 更正（007 brainstorm 偵查實測）：該測三 assert 只查 `!outside.is_empty()`／`!inside_protected.is_empty()`／`inside.len()==candidates.len()`，刀 B 註冊十支後候選外仍餘 audit 域 5 列 ⇒ **續綠、不轉紅**（前句「由綠轉紅」為誤）；流失的是「候選外 protected 列」（現恰 1＝seed 68）的靜默鑑別力。處置不變＝種合成候選外 protected 探針列、把 outside_protected≥1 升成真 assert（刀 B 工程自決）。
- B-119｜「reload 呼叫點不得持讀鎖、須在 commit 之後」紀律**無源碼掃描守門**：`authz_entrypoint_lint` 的 RELOAD_CALL_FILES 名冊只守呼叫點檔集恰等、不守呼叫時序語意（現靠 enforce.rs doc＋各呼叫點碼註＋review）；候選修法＝該 lint 加語法面掃描（reload_enforcer 呼叫行的上下文不得持 `.read()` guard／須在 txn commit 之後）——heuristic 難精確、先掛候選（006-authz-governance T036 新登）｜下一把動 authz_entrypoint_lint 的刀
- B-120｜**events.jsonl 的 summary 寫錯無 erratum 出口**：append-only 紀律（ADR 0012 決定 5）下既有列絕不編輯、Lint03 只擋長度——事實性錯誤只能靠後續事件的 notes 順帶澄清、無形制化通道；候選修法＝規格化「澄清事件」形（misc 事件帶 `erratum_of` 欄指向被澄清列＋docs-sync 對賬）——待真踩到第一筆錯 summary 再落（006-authz-governance T036 新登）｜下一次動 events 形制或 docs-sync 事件面的刀
- B-133｜**repo 位於 drvfs（`/mnt/d`、v9fs）＝pre-commit 全鏈 I/O 稅的根因，遷至原生 WSL 檔案系統可再得約 587× 的 I/O 面**（ADR 0061 留帳的根治面）：2026-08-25 受控微基準——同一批 269 檔 stat+read，drvfs **2845ms** vs 原生 ext4 **4.8ms**，每檔約 10.56ms（L-062）。★**CLAUDE.md §11 自己就寫著** performance-sensitive path 屬原生 WSL 路徑或容器卷、不是 `/mnt/*`——repo 本身正是這樣的路徑，只是當初未把它算進去。★**注意 `/home/anew/x_Project` 不是原生副本**：它是 symlink 指向 `/mnt/d/AnewSpaces/x_Project`（2026-08-25 實測 device+inode 相同），誤以為那裡已有一份 ext4 副本會導致錯誤結論。★**屬拓樸調整、須走一次性遷移程序**（CLAUDE.md §2：brainstorm 或 spec 附 Risk／Guard／Rollback 三欄表）。連動面至少四項：①兩個 submodule worktree 的 `.git` 檔記的是**源倉絕對路徑**、源倉本身也要一併搬 ②`docker-compose*.yml` 的 bind mount 路徑③Windows 側工具鏈與編輯器存取改走 `\\wsl$`（該向存取會變慢——遷移是把成本從 WSL 側移到 Windows 側，非純賺）④CDP 走查、rev4 對照環境（`../fork260509-rev4/`）的相對路徑假設。★**現況已不急**：ADR 0061 的 A+B 已把全鏈壓到 13.09s（3.3×）、遠低於 45s 警戒；本條是「若日後 repo 規模再成長一個量級」的備案。｜pre-commit 全鏈再度逼近 45s 警戒線時，或有其他理由要動 repo 拓樸時

- B-131｜**menu 治理清單頂層 >100 列時資料不可達、且凍結後無從得知**（ADR 0060 殘餘一）：後端 `get_menu_list` 之 `size` 預設 `MENU_PAGE_SIZE_MAX`（100）並 clamp，`paginate_top_level` 只切頂層 ⇒ 頂層逾 100 時第 101 列起不回；前端治理清單走無參呼叫、且分頁列已依 ADR 0060 凍結（頁碼恆 1、每頁 0、上鎖）⇒ 使用者看到 prefix「共 N 条」卻只有 100 列、**且沒有任何可操作路徑抵達其餘列**。★**改前同樣不可達**（api 無參、換頁不重取資料），凍結未使之惡化、但也未解決——ADR 0060 逐字載明此殘餘。★候選處置：①治理清單改不截斷（後端為該路徑放寬或取消上界、樹表本就一次取全樹）②total 超過實得列數時於頁面給明示訊號（非靜默）③加機器守：斷言頂層列數 < MENU_PAGE_SIZE_MAX、逼近即紅。★**現況距觸發尚遠**：2026-08-25 實測頂層 11 列（`select count(*) from sys_menu where deleted_at is null and parent_id is null`）｜頂層列數逼近 100 時，或下一把動 menu 治理清單資料源的刀
- B-132｜**回收桶模式的每頁筆數初始顯示 100（治理清單殘留值經參數通道洩漏）**（ADR 0060 殘餘二）：鏈路＝治理清單載入 → 後端回 size=100 → `hooks/common/table.ts` 之 `onFetched` 執行 `pagination.pageSize = data.pageSize` → 切「顯示已刪除」時 `onPaginationParamsChange({pageSize:100})` 把 100 寫進 `deletedSearchParams.size` → 回收桶請求帶 size=100、後端照回 ⇒ 下拉顯示 100（不在 `pageSizes [10,15,20,25,30]` 內）。★**屬既有缺陷、非 ADR 0060 所致**：改前兩模式同顯 100 故不可見，改後治理清單顯示 0、落差始外顯（2026-08-25 CDP 實測兩態皆錄）。★嚴重度低於 B-097 原症：回收桶分頁可操作，使用者改了即生效、不回彈 ⇒ 只是初始值怪。★候選處置：①切模式時重置 `pagination.pageSize` 為 hook 預設 10（一行，但動 005 期 watch 標記塊）②治理清單模式不讓 `onFetched` 污染 pageSize（需動共用 hook＝爆炸半徑五頁：ip-rule／policy-archive／menu／role／user）。｜下一把動 menu 頁分頁面或 `hooks/common/table.ts` 的刀
- B-124｜**`metric_value` 六份同形＋`handler/role.rs` 一處手搭 inline oneshot 待收攏**（B-110 oneshot 收攏的續集、維護批 A U2 查定）：①`fn metric_value`（單一方法鏈純函式、自 render 文本取 series 值）現有六份——`auth/enforce.rs`／`handler/menu.rs`／`handler/role.rs`／`handler/policy_archive.rs`／`model/facade/sys_role.rs`／`model/facade/sys_menu.rs`（枚舉式＝`grep -rn "^\s*fn metric_value" --include=*.rs server/`、行首錨定必要：正典 doc 自身含該字面）；B-110 已證跨檔 test helper（`#[cfg(test)] pub(crate)`）取用得到，「各住私有模組看不見」不再是理由，現行不收係**取捨**（零共用狀態、無跨檔同步面）——判準與射程的正典寫在 `handler/menu.rs` endpoint_tests 該處 doc、本條只承載「日後若改判要收，落點＝`model::test_db`」。★**2026-08-25 帳面更正：原②「`handler/role.rs` 一處手搭 inline oneshot」已不成立、自本條出列**——該檔 enrich 讀端測早已走 `test_db::oneshot_json`、`Body`／`HttpRequest`／`AUTHORIZATION`／`ServiceExt` 四個 import 亦已退場（維護批 A U2 之 B-110 收攏一併收了，pin fd23de9）；枚舉指令＝`grep -n "\.oneshot(\|Body::empty\|Body::from" rust-api/server/src/handler/role.rs` → 零命中（2026-08-25 量測）。★本條殘餘射程自此**只剩①**（`metric_value` 六份同形之取捨）｜下一把動 `metric_value` 面的刀
- B-125｜**`handler/auth/logout.rs` 的「commit 之後才讀 TTL」同樣無機器守**（B-056 收 detect_reuse 那條腿時查定、同形未覆蓋）：logout 的 :123~:158 與 `refresh.rs::detect_reuse` 逐形相同（`revoke_row` ＋ `session_event::insert` ＋ `txn.commit()`，TTL 讀在 commit 之後走 `state.db`），B-056 建的 PG 層 fault-injection seam（`test_db::real_db_single_with_lock_timeout` ＋ `TableLock`）在**語意上覆蓋得到**，卡的是建構面：`detect_reuse` 是同模組私有 fn 可直呼，logout 是收 extractor 的 handler、必須經 app 打端，而 `test_db::real_app_and_state_with` 內部自呼 `real_db()`、**不接受注入 db** ⇒ 要讓 app 跑在單連線 lock_timeout 池上得替它加一個收 db 的建構腿（或第三個 AppState 建構點——正是 B-054／B-101 兩批收攏壓下去的東西）。★危害同 L-016：ttl 讀若被搬回 txn 內並 `.ok()`，PG 把交易推入 aborted 態後 COMMIT 靜默降級成 ROLLBACK ⇒ 「回 8888 但什麼都沒撤」、零訊號。候選處置＝①`real_app_and_state_with` 擴一個收 db 的薄殼（`_with_db`）後補測 ②維持現狀、僅倚賴 refresh 側那支守（同段碼註已互引）｜下一把動 `real_app_and_state_with` 建構面或 logout 測試面的刀
- B-127｜**`db_status_to_wire` 的反向鏡像仍是兩份逐位同形私有拷貝**（B-108 收攏的補集、刀 B 前置維護批 U1b 碼品質輪查定）：B-108 已把 DB→wire 方向收進 `handler/common.rs`（全樹恰一份），但**wire→DB 方向**仍分住 `handler/role.rs::wire_status_to_db` 與 `handler/menu.rs::wire_two_value_to_db`——正規化識別字後 body **完全相同**（`match s.map(str::trim) { Some("1")=>Some(1), Some("2")=>Some(2), _=>None }`），兩者 doc 還互相指名「同一條線」。★當時不收＝正確的 scope 紀律（B-108 的 charter 只寫了 `db_status_to_wire`、順手擴張會讓收攏批無邊界），非遺漏。★收攏落點與 B-108 同（`handler/common.rs`）；★命名須一併裁：兩支現名不同形（一支帶 `status`、一支帶 `two_value`），收攏時要選一個能同時涵蓋 role.status 與 menu 兩處二值欄的名字，並把兩處 doc 的「同一條線」互指句改寫成指向共用件｜下一把在 handler 層動 status／二值欄 wire 映射的刀（刀 B 的 user 域 status 面高機率觸發＝第三消費者）
- B-128｜**`eslint` 對 `src/**/*.ts` 零覆蓋 ⇒ 任何「只跑 eslint 驗 .ts」的單元指令都是假綠**（刀 B 前置維護批 U3 碼品質輪查定）：`pnpm exec eslint --print-config src/service/request/index.ts` 回 `undefined`＝flat config 無匹配設定，實跑則以「File ignored because no matching configuration was supplied」計一個 warning ⇒ `--max-warnings=0` 下 rc=1（對未改動的 `src/service/api/rev5-role-admin.ts` 同樣重現＝既存現象、非某次改動所致）。.ts 面的實際檢查由 `oxlint` 承接（`package.json` 之 `lint` ＝ `oxlint --fix && eslint --fix .`）。★危害＝**方向是假綠而非假紅**：單元若只跑 eslint 驗 .ts，得到的 rc=1 會被讀成「我的檔有問題」而去繞過，或改用 `--max-warnings=1` 之類的調整而讓真 warning 一併被吃掉。★候選處置：①編排 script 的前端驗證段對 .ts 改指 `pnpm exec oxlint <file>`（唯讀、不帶 --fix）②於 CLAUDE.md §2 範本或 RUNBOOK 註明 eslint／oxlint 的分工射程③評估補 flat config 的 .ts 區塊（動 `eslint.config.js`＝fork-delta 修改型、須授權判定）｜下一把動前端驗證指令或 eslint 設定的刀
- B-129｜**三顆授權 modal 的「換角色開啟時不清上一角色顯示狀態」＋`getHome` 無請求世代**（B-116 的鄰接殘餘、刀 B 前置維護批 U3 碼品質輪查定；★兩件同域同修法家族、合帳）：①`init()` 未重置 `rawChecks`／`protectedIds`（button 的 `codes`、endpoint 的 `endpoints`／`leafMap` 同理）⇒ 換角色開 modal 時，在自身回應落地前會顯示**上一角色**的勾選集與鎖定狀態；確定鈕已由就緒守停用 ⇒ **無資料風險、純視覺誤導**（U9／U9b 既有形，B-116 未加劇）。修法＝`getChecks()` 起手與 `checksLoaded=false` 同處清空（一～二行×三檔）。②`menu-auth-modal.vue` 的 `getHome()`（`fetchGetRoleHome(props.roleId)`）同為角色維請求卻不在 B-116 射程內（該條文逐字只寫 `getChecks`、U3 守住 scope 是對的）⇒ 亂序時 `home` 可能持續顯示上一角色的值；危害低於 checks——`updateHome` 只在使用者**顯式**改選時才寫、且恆帶當下 `props.roleId`，不會靜默寫錯角色，但顯示面會誤導。修法＝比照加 `homeReq` 世代序號（約 4 行）｜下一把動三顆授權 modal 的刀（刀 B 若照抄該形，宜同批修）
- B-134｜首登強制改密（rev4:015-pwd-custody 整包：`needChangePwd` 欄＋`pwd_gate_mw` 白名單硬閘＋強制改密 constant 頁＋`router/guard` 三行攔截）——007 brainstorm Q02 拍「刀 B 不做」：憲法射程（router/guard／build/plugins／store/auth 三處零對應樹須新 inline 授權）與 003「needChangePwd 不帶回」翻案足以自成一刀；`sys_pwd_custody` 表已在基線、刀 B 只借其時戳做設密冷卻（Q20）、不做 EXISTS 判定★**2026-08-27 刀 B U0 複核**：射程關係確認不變——刀 B **不做**首登強制改密整包，`sys_pwd_custody` **只借時戳**做設密冷卻（`touch` upsert＋`last_set_at`、零 EXISTS 經手判定；U0 實測該表現況零列）。★連帶取得憲法層保留位：v1.9.0 島 I 之 **I6 位刻意空缺**即為本條（rev4 之「密碼經手與首登強制換密」、rev4:ADR 0067），header 括號逐字載「日後兌現時回填該位」⇒ 本條動工時不需重新爭取條號、直接填 I6。｜自助改密上線（刀 B 收刀）後的下一把安全硬化刀
- B-135｜**`test_db::UserCleanup` 兩個建構子並存＝過渡形，待收成單一「雙名冊」建構子**：007 T021 為了補業務鍵腿（`user_name` 測試前綴）新增 `with_name_prefixes(ids, name_prefixes)`，但既有 12 個呼叫點仍走 `new(ids)`。終態應照同檔 `RoleCleanup`／`MenuCleanup` 的雙名冊形收成一支——統一需同批改 `rust-api/server/src/model/facade/sys_role.rs`（3 處呼叫點）與 `rust-api/server/src/handler/policy_archive.rs`（1 處），兩者皆在刀 B U1 的允許檔清單之外，故該單元刻意留成薄殼過渡形（型 doc 已逐字記載過渡與統一方式）。★危害＝低（兩支語意不重疊、無假綠面），純屬名冊形不一致；★不做的代價＝日後新增 user 相關測時要先分辨該用哪一支。｜下一把動得到 `facade/sys_role.rs` 或 `handler/policy_archive.rs` 的刀（本刀 U2 起即可能觸及）
- B-136｜**手機欄「≤32」的長度守門在契約面缺一格、四個權威互相矛盾（需 user 裁定）**：spec FR-008 逐字寫「手機（選填、≤32）」、brainstorm G17 拍「手機只驗長度」，但 ①`contracts/wire-user-admin.md` §3 addUser 的守門序**沒有這一格**（形制→唯一→信箱格式→roleIds→N⊆A→政策→冷卻）②`contracts/msg-keys.md` 的 `backend.biz.user.*` 二十鍵**無**手機相關鍵 ③DB `sys_user.user_phone` 為 `character varying` **無長度上限**（`character_maximum_length` 為 NULL、2026-08-28 實測）⇒ 超長值不會撞欄寬、不會 5000，只是原樣落庫 ④rev4 藍本（`rev4:handler/user.rs`／`rev4:facade/sys_user.rs`）**亦無**任何 phone 長度守門與對應 msg key。★本刀 U2 未自行發明新鍵（發明＝憑空造 user 可見拒因、且會讓 msg-keys 由 20 鍵變 21、T051 計數連動）。★三個處置選項：①補 `biz.user.userPhoneInvalid`（契約 §3 加一格＋msg-keys 加一鍵＋四處 i18n＋handler 守門＋測）②把 spec FR-008 的「≤32」改判為欄位描述而非守門要求（立 ADR won't-fix）③維持現狀不表態（不建議——兩份權威留著互相矛盾）｜刀 B 收刀前（T073 一併裁定）或下一把動 user 寫端的刀
- B-137｜**`R_SUPER` 在生產面有兩份獨立字面常數**：`model/facade/sys_user.rs` 的 `SUPER_ROLE_CODE`（本刀 U2 新增、seed 保護用）與 `auth/no_escalation.rs` 的 `ROLE_SUPER`（本刀 U1 新增、`ActorScope::All` 判定用）。★兩側**各自有機器釘**（各配一支對 seed 的字面斷言），任一側漂移即紅，故非假綠面；但同一個 seed 角色碼在生產碼有兩個宣告源，日後改碼名要記得改兩處。候選處置＝收攏為單一 `pub const`（住 facade 或 model 層）、兩側改 import；★收攏時務必保留**兩支**釘值測（合併成一支即失去「兩個消費面各自對 seed 對賬」的鑑別力）｜下一把同時動得到 `facade/sys_user.rs` 與 `auth/no_escalation.rs` 的刀
- B-138｜**`ilike_contains` 在 facade 層有三份就地拷貝、宣告源不唯一**：`facade/sys_role.rs`（`fn ilike_contains(column_sql, needle)`）／`facade/sys_user.rs`（同簽章、本刀 U2 新增）／`facade/sys_ip_rule.rs`（`fn ilike_contains(needle)` 窄化變體）。成因＝收攏得動 `model/facade/mod.rs`（跨表共用件的家、`violated_constraint`／`now_ts` 所在），而該檔在當時執行單元的允許改動面之外 ⇒ 就地重打、收攏留帳。★**三份各自被自家 facade 的查詢行為測走到、非假綠面**；問題在紀律面——三份的 escape 規則有微妙前提（反斜線最先替換、不另寫 `ESCAPE` 子句、sea-query 0.32 的佔位符為 `$1` 而非 `?`），其中一份被改時另兩份不會有任何測試提醒。★候選處置＝收攏為 `model/facade/mod.rs` 的單一 `pub(crate)` 共用件、三側改 import；★收攏時務必**保留三支各自的行為測**（合併成一支即失去「三個消費面各自對自家欄語意對賬」的鑑別力，與 B-137 同一注意事項）。｜下一把動得到 `model/facade/mod.rs` 的刀
