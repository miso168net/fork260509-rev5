---
promoted_to: tools/docs-sync.py 之 TestGateWiring（樁工具乾跑真 hook；trigger＋day1 成對案）
---
- **L-040**｜**守門動作住在 shell／設定面時，「工具本身有 self-test」完全擋不住「它根本沒被跑」**——004 U-I 實暴且經兩道變異證實。新守門 `view-render-guard` 的 self-test 隨每次 `check` 連帶跑、非 vacuous，但把 `.githooks/pre-commit` 裡呼叫它的**整段拆掉**後，`docs-sync.py test`（494 支）與 `lint` **仍全綠** ⇒ 守門實質下線而零訊號。★**把斷言寫進工具自身的 self-test 對本失效模式零效果**：self-test 只隨 `check` 跑，而 `check` 的唯一觸發點正是被拆掉的那一段——**循環依賴**。守接線的案**必須住在別的地方**（本 repo 慣例＝`docs-sync.py` 的 `TestGateWiring`，以樁工具乾跑真 hook 檔文、比對 argv 序列）。★**第二個更隱蔽的坑：測試沙盒不保真，新案會恆綠**。`TestGateWiring` 的 `base-web` 佔位原本是 `_wfile()` 寫的**檔案**，而 hook 的 Day-1 條件是 `[ -d base-web/src ]` ⇒ 該條件在十餘次乾跑中**恆假**、整段從沒被執行過一次；此時補的觸發案會通過，但通過的理由是「沒跑到」而非「跑對了」。★**修法有個反直覺的轉折**：把佔位改成**普通目錄**會更糟——`_run` 走 `git add -- base-web`，普通目錄被展開成 `base-web/src/...` 逐檔入 index，hook 的 `grep -qxF 'base-web'`（整行精確比對）當場落空，**連 fork-delta 與 wire-schema 兩段也一起靜默不觸發**（實測：staged 只剩 BASE 三行、既有案跟著紅）。正解是建**巢狀 git repo**（本 repo 已有 `_init_sub()`）：才會被記成 mode 160000、staged 路徑逐字為 `base-web`，與真 submodule 同形，目錄條件與路徑條件同時成立。★**驗收 MUST 兩道變異、缺一不可**：①拆掉整段 → 紅（守「被刪除」）②把 Day-1 條件由 `-d` 反轉成 `! -d` → 紅（守「還在、但永遠不跑」）。②才是真正難抓的那個——區塊還在、讀起來正常、code review 看不出來；本 repo 既有的 `fork-delta`／`entity-drift` 兩處都以「trigger_conditions ＋ day1_skip」**成對案**擋它，新守門一律照抄該形。

