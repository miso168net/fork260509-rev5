---
promoted_to: rust-api/server/src/model/mod.rs 之 SessionIdCleanup 守衛型 doc 名冊（名冊由 grep 產生、住型 doc）
---
- **L-045**｜**清理義務被某個檔案的私有 fixture 就地解掉之後，那個解法會把問題的全域性一起藏起來——後續每一支同型測試都得自己重新踩一次**：004 U-L 補 `sys_user.session_id` 守衛時實暴。機制：login 第⑨步在 single-session 生效時寫 `sys_user.session_id`，而 `sys_user` **不在** `schema-gate.py` 的 `RUNTIME_APPEND_TABLES` ⇒ gate2 對它原位逐列 diff、殘值即紅。這件事**早就被解過一次**——`handler/auth/refresh.rs` 的私有 `SessionPolicyFixture` 在 Drop 時順手把 `session_id` 歸 NULL，因為那個檔正是會把 single-session 翻 `on` 的檔、作者當場撞到。但它**寄居在該檔的測試模組內**、沒有上移成共用守衛 ⇒ `model::test_db` 的守衛名冊裡看不到它，於是在**別的檔**新增成功登入測的人，既不會知道有這個義務、也沒有現成的東西可掛。 ★**實測枚舉**（以 `grep -rn '"123456"\|SEED_PASSWORD'` 併 `grep -rn 'post_login\|login_for_token'` 掃 `src/**`、逐支落地確認終局，不是讀 task 文字）：走成功登入路徑的真庫測共 **14 支跨四個檔**——`login.rs`（6 支）／`refresh.rs`（5 支）／`user_info.rs`（1 支）／`throttle/mod.rs`（2 支）。其中 `refresh.rs` 的 5 支有 4 支被那個私有 fixture 蓋到 ⇒ **該掛共用守衛者 10 支**；**其中 4 支（跨 3 個檔）完全沒有守**（`refresh.rs::t041_default_inherit_off_second_login_does_not_kick` 自己沒掛 fixture、`user_info.rs::t019_three_accounts_login_then_user_info_four_fields`、`throttle/mod.rs::us4_soft_zone_rejections_do_not_consume_bucket_success_still_possible`、`throttle/mod.rs::us4_sliding_window_resets_on_success`）。 ★**今日不爆、但那是設定相依的僥倖**：seed 是 `session_policy=inherit`＋`single_session_default=off`，而後者是 **runtime 可改值**（走查前置翻 `on` 是既有實務）。已實測坐實兩向——翻 `on` 後拆掉守衛跑那支 t019：`sys_user` 留下 `session_id` 殘值、`schema-gate check` 當場紅在 seed 逐列 diff 的 sys_user 那一列；掛回守衛再跑：殘值為 0、四項全綠。 ★**兩條防法**：①**「掛到全部走 X 路徑的測試」這種名冊，MUST 由 grep 產生、不得由 task 文字的『已知至少 N 支』產生**——後者是散文枚舉，寫的人只數得出他當時想得到的那幾支（本次 task 文字寫「已知至少兩支」，實得四個檔）。★**本條在同一個單元內當場再犯一次、值得單獨記**：初版名冊（守衛型 doc＋本條＋B-084 三處同一組數字）寫成「11 支／`login.rs` 3 支」，且把 `t069_chain_rejection_precedes_the_throttle_precheck` 列為已掛——實際掛的是**同前綴**的 `t069_chain_rejection_precedes_password_hashing_with_no_success_side_effect`（前者用臨時帳號名＋錯密碼、永遠到不了第⑨步，依判準根本不該上榜），而真正漏掉的是 `t048_source_dimension_is_wired_and_its_rejections_consume_no_bucket`（其第 ④ 步「拒絕之後仍登得進來」是真的成功終局）。 ★**同一單元內第三次再犯**（U-L 的 review 輪抓到）：修正後的名冊寫「12 支／`login.rs` 4 支」，仍漏 `login.rs` 的 `t019_login_disabled_account_1000_with_identified_audit_row`（uid 3）與 `t019_login_lock_recheck_failure_audit_row_survives_rollback`（uid 3）——**漏因與前兩次都不同**：判準的前半句（「走成功登入路徑」）枚舉得很仔細，後半句（「**外加 L-036 那一形**＝密碼正確卻斷言不得成功」）是名冊作者**自己寫進判準、卻沒拿它再掃一遍**；而這兩支正好在該單元的空間邊界之內、當場就掛得上，「未掛的都在邊界外」那句連帶成假。⇒ **防法③：判準若是「A 形**或**B 形」的複合式，MUST 對 A 與 B 各跑一次獨立枚舉再取聯集**——單次掃描只會照著寫的人心裡的主形（A）收，附加形（B）恆漏。本例 B 形的機器化查法＝「seed 帳號名＋正確密碼」的全部命中裡扣掉終局為成功的那些，剩下的即是 B 形（另須逐支排除「正確密碼配臨時帳號名」與「直呼 `authenticate` 不經 handler」兩類假命中——排除理由已逐條寫進守衛型 doc，否則下一位覆核者會重查一遍）。⇒ **grep 之後還有一步會出錯**：`grep -rn '<守衛名>'` 只回行號，把行號對回函式名時若用眼睛湊前綴就會湊到隔壁那支；MUST 把命中行號夾在 `grep -n 'async fn'` 的相鄰兩個函式邊界之間判定。②**發現某個清理義務時，先問「這個義務是這個檔專屬的，還是這條程式路徑專屬的？」**——是後者就 MUST 上移共用設施（`model::test_db`）並在型 doc 寫明「哪些測試該掛」，否則它就是一顆只有原作者知道的地雷。這與 B-055 把 `SessionEventCleanup` 上移是同一個動作，差別只在那次有人想起來做。 ★**識別法**：某個 `#[cfg(test)]` 模組內的私有 fixture，其 Drop 還原的欄位**不屬於該檔的被測物**（本例＝refresh 的測試在還原 login 寫的欄位）＝八成是本形。 ★**終局補記（004 收刀前 final holistic review）**：那 4 支已於**同一刀**補齊 ⇒ 該掛的 10 支**全數掛上**，承載它們的 BACKLOG 條目（B-084）依「完成即刪列」已刪；碼註三處的引用一併改指`model::test_db::SessionIdCleanup` 的守衛型 doc（＝名冊唯一的家）。★**這一步也是本條的一部分**：名冊寫「未掛 N 支、殘餘落 B-NNN」時，那個 B-NNN 一旦關帳，碼註就成了指向空號的引用——**名冊 MUST 指向不會被刪的家（型 doc），不是會被刪的待辦編號**。


