---
promoted_to: 已補 `specs/007-user-password-admin/quickstart.md` §6 步驟 7 之走查後清理契約（判準形，非清單形）；★候選晉升位＝**CLAUDE.md §2 或 RUNBOOK 的常駐程序**（本條的重點正是：晉升到「下一份一次性文件」等於沒有晉升），機制面候選＝走查前後全表基準對賬的小工具（B-147）；★2026-08-30 再晉升為機制：RUNBOOK §9c「CDP 真登入走查的環境還原契約」（常駐契約、判準形六步、讀者是每一刀）＋`tools/walkthrough-baseline.py`（snapshot／diff 全表基準對賬——表列數＋序列＋redis 前綴鍵數、全部現算零手抄名冊；B-147 候選①＋②並行落地）；★2026-08-30 B-091 盤點（已兌現：RUNBOOK §9c 常駐契約＋tools/walkthrough-baseline.py、B-147）
---
- **L-071**｜**把教訓「晉升」到下一刀的一次性文件裡，它的半衰期就只有一刀——L-055 時隔六天原樣復發**：[[L-055-runtime-append-residue-blows-next-full-suite]] 把「真登入走查會在 runtime-append 表留 committed 殘列、下一輪全量才紅」寫得很完整，防法也具體，其 `promoted_to` 寫的是「006 quickstart §4 走查後清理句（後續刀的 CDP 走查契約**沿抄該句**即帶防法）」。★**2026-08-30 實暴**（本刀 final holistic 的 CDP 步驟 3／4）：007 的 quickstart **一個字都沒沿抄**（`grep 清理|殘列|setval|TRUNCATE` 零命中），於是我手上沒有可執行的清單，只能自己推——而我推的正是 L-055 ① 明文點名的那個盲點：**「schema-gate 豁免這四表」⇒「這四表不用清」**。結果＝種子面逐值還原、三閘全綠，全量測試卻兩支紅。★**本刀的具體形是 L-055 兩條爆線的第三種組合**：我把 `sys_user_id_seq` 復位回 3（種子面還原的正確動作），而 `sys_operation_log` 還留著 `user_id=4`（走查建的 alice）的稽核列 ⇒ 測試新建的使用者拿到 id=4、`op_log_rows(new_id)` 撈到那筆走查殘列，「稽核恰一列」變兩列；另一支是 `sys_token` 殘留 uid 3 的憑證列撞「uid 3 不得有憑證列」。**兩支都不是碼缺陷，但都要花一輪全量測試（約 3.5 分鐘）＋一輪歸因才看得出來。** ★**根因兩層**：①清單式防法會隨情境失效——006 撞的是 auth seq × `sys_token`，本刀撞的是 user seq × `sys_operation_log`，照 006 的清單逐條做**也擋不住**本刀這一種②真正的問題是**晉升位選錯**：一次性文件（某一刀的 quickstart）的讀者只有那一刀，「下一刀沿抄」是一個沒有執行者、沒有機器守、也沒有人會被提醒的義務，它和「靠人記得」沒有本質差別。★**防法**：①走查**前**取全表基準（列數＋seq，含 runtime-append 四表**與 redis**），走查後**逐值比對**——把防法從「照清單刪」改成「前後相等」這個**不變式**，情境再變也不失效②清理面的定義是「走查期間被寫過的全部表」，**與任何閘的射程無關**——「某道閘不檢查 X」只說明那道閘的射程，不說明 X 無害（同 [[L-069-visibility-widening-punches-through-a-roster-gate]]：閘的判準與它宣稱守的不變式之間永遠有距離）③走查收尾**必跑容器內全量測試**，不能以三閘綠代替——「三閘綠而全量紅」正是本坑的招牌徵狀。★**一般化——寫 `promoted_to` 時要問的一句**：**這個位置的讀者，是不是恰好就是下一個會踩坑的人？** 晉升到一次性文件＝一刀的壽命；晉升到常駐程序（CLAUDE.md／RUNBOOK）或機器閘，才跨得過刀與刀之間的縫。
