---
promoted_to: `rust-api/server/src/model/mod.rs` 之新守衛 `test_db::SeedUserRestoreCleanup`（arm 拍快照→Drop 原樣寫回）；★候選晉升位＝CLAUDE.md §2「補守門必做變異測試」旁一句——變異**破壞性**守門前先確認該破壞有還原守衛；★2026-08-30 B-091 盤點（仍待：同 L-063、CLAUDE.md §2 現無該句；下一把動 §2 範本的刀併入）
---
- **L-065**｜**對「破壞性守門」做變異測試，那一發破壞是真的會發生——而 dev 庫的 seed 列一旦被改，`schema-gate` gate2 就永久紅、只能人工 SQL 善後**：本 repo 的清理守衛家族全是 **DELETE 式**（`RoleCleanup`／`UserCleanup`／`CasbinCleanup` 皆為「刪掉測試造的列」），且 `new()` 還帶**結構性拒收 seed id** 的斷言——這個設計對「測試造列」是對的，對「測試**改動既有 seed 列**」卻結構上救不了：守衛不會、也不該去刪 seed 列。★**2026-08-28 實暴**（本刀 U2／T023 的 seed 保護守門測）：把 `guard_seed_delete` 改壞做變異驗紅，那一發**真的**軟刪了 seed Super（id 1）並硬刪其 `R_SUPER` 指派列；`sys_user`／`sys_user_role` 皆在 `RUNTIME_APPEND_TABLES` 收窄集**之外** ⇒ gate2 seed 逐列全等當場紅，且沒有任何守衛會把它補回來——只能人工 SQL 還原十二個可寫欄與那筆指派列。★**防法＝快照式（非刪除式）守衛**：`test_db::SeedUserRestoreCleanup::arm(ids)` 於測試起手拍下該批 seed 列的全欄快照＋其指派列，`Drop` 時原樣寫回（有則 UPDATE、缺則 INSERT）。★**一般化**：清理守衛有兩種語意——「刪掉我造的」與「還原我改的」，前者對 append 型測試、後者對 mutate 型測試，**互相取代不了**；動到既有列（尤以 seed）的測試 MUST 掛後者。判別問句：這支測失敗時，庫裡會多出東西還是會少掉／變掉東西？少掉或變掉＝需要還原式守衛。
