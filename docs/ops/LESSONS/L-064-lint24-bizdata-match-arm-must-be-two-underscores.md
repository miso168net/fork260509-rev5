---
promoted_to: `rust-api/server/src/error.rs` 之 `BizData` 三處 match 臂就地寫成 `(_, _)`（碼內既成事實）；CLAUDE.md §2 單元收尾②已載——rust 單元自驗另跑 docs-sync.py lint、cargo 綠與 lint 綠是兩件事（2026-08-31 維護批 W2）
---
- **L-064**｜**`AppError::Biz`／`BizData` 的窮舉 match 臂寫成 `(..)` 會被 Lint24 判成「構造點無法靜態解析」而 fail-loud——且 `cargo test` 全綠時它完全看不見**：`tools/docs-sync.py` 的 Lint24（backend msg key 對賬）以 `RE_I18N_SITE` 匹配 `AppError::Biz(`／`AppError::BizData(`，再以 `RE_I18N_ARM` 把「match 臂而非構造點」排除掉；而該排除樣式只認 `(_)`／`(ident)`／`(_, _)`／`(ident, ident)` 四形，**`(..)` 不在其中** ⇒ 落不進排除面、被當成一個無法靜態解析 key 的構造點而報 ERROR。★**2026-08-28 實暴**（007 T007 加 `AppError::BizData(Cow<\'static, str>, serde_json::Value)` 變體、補三處窮舉臂時）：`[ERROR] Lint24｜rust-api/server/src/error.rs:96｜AppError::Biz/BizData 構造點無法靜態解析（內文開頭：..) => code::BIZ, …）`。★**修法＝把臂寫成 `(_, _)`**（本刀已採），毋須動 `tools/docs-sync.py`——`(..)` 只是 Rust 的等價簡寫，改成逐位元組的 `(_, _)` 零語意差異，而工具側放寬樣式反而會擴大「真構造點被誤排除」的面。★**真正該記住的一般化教訓**：這一類漏網**只有 `python3 tools/docs-sync.py lint` 跑得出來**，rust 單元若只驗 `cargo test`／`cargo build` 就會把它帶進 commit（pre-commit 會擋，但那是在 commit 訊息都寫好之後）——單元收尾②的「三閘」不是修辭，`cargo` 綠與 lint 綠是兩件獨立的事。相關：[[workflow-unit-orchestration-shape]] 的「不採信 agent 回報」。
