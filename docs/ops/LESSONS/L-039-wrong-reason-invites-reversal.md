---
promoted_to: rust-api/server/src/handler/auth/login.rs 拒絕腿碼註＋model/facade/sys_login_attempt.rs（「原記理由已證偽、勿反向修改」標注）
---
- **L-039**｜**決策是對的、但被記下的理由是未經查證的推測——而錯誤的理由會被下一個人拿來推翻正確的決策**：004 U-M 的 `login` 拒絕腿擺在「形制閘之後、precheck 之前」，implementer 給的理由是「擺到形制閘**之前**會讓『超長帳號名＋敵意鏈』去撞 `attempted_user_name` 的**欄寬**，白換一則 degraded warn」。擺位本身正確，理由卻**整個不存在**：品質輪的 fix agent 以容器內唯讀 psql 實查——`sys_login_attempt.attempted_user_name` 是 **`text`、無長度上限**，該表 `pg_constraint` 恰 6 條（五條 NOT NULL＋一條 PK）、**零 CHECK** ⇒ 不會 22001、`record_attempt` 不回 Err、`degraded="db_write"` 不會發。★**主線（我）採信了這個理由而未查證**，它就這樣進了碼註。★**為何危險**：碼註是給「日後想改這裡的人」看的。一個讀者若去驗證那個理由、發現它不成立，最自然的結論是「那這個擺位沒有理由，可以前移」——**錯誤的理由比沒有理由更糟**，因為它把一個真實約束偽裝成一個可被證偽的藉口。★**真正的理由**（fix agent 補上的）：形制閘先擋逾 `throttle::LOGIN_USER_NAME_MAX`(64) 的帳號名；本腿若前移，一發 `userName` ≈2MB（axum 預設 body 上限 2MB，已於容器內 registry 逐字查證 `axum-core-0.5.6/src/extract/default_body_limit.rs`）＋敵意鏈，即在 **append-only** 的 `sys_login_attempt` 落一列 MB 級 `attempted_user_name`，**無錯誤、無 warn、零 metric**；而 FR-050 的來源維節流封的是**列數**不是**列寬**、接不住這一形 ⇒ 擋線只有形制閘。★**防法**：①凡碼註寫「不這樣做會 X」，X **MUST 是查證過的事實**——查 schema、查 constraint、查 crate 原始碼，或直接寫一支測把 X 釘成可執行事實；查不動就誠實寫「推測、未驗」。②主線覆核 agent 報告時，**「理由」和「結論」要分開驗**——結論對不蘊含理由對，而我這次只驗了結論。③修正這類註解時 MUST 顯式標注「原記理由已實測證偽、勿再據以反向修改」，把下一位讀者的錯誤路徑先堵住（fix agent 這次就是這樣寫的）。 ★**同形再現（004 U-H）**：`model::test_db` 的 sequence 名冊 doc 寫「三支——（本刀只寫 `sys_token`／`session_event`／`sys_login_attempt` **三表**）」。T037 之後本刀實際寫**四**表，**結論（名冊仍三支）依然對、理由卻已假**——真正的判準是「該表在不在 `RUNTIME_APPEND_TABLES` 收窄集內」（在集內者 gate2 不逐列 diff、sequence 被推進無妨），不是「本刀寫了哪些表」。★這一形特別難抓：**結論正確**會讓覆核者停在「這句沒問題」，而錯的理由要等到有人依它推論（「本刀寫了第四表 ⇒ 名冊該補第四支」）才會出事。

