---
promoted_to: specs/006-authz-governance/quickstart.md §4 走查後清理句（2026-08-24 補三張 runtime-append 表清列＋三 seq 復位；後續刀的 CDP 走查契約沿抄該句即帶防法）
---
- **L-055**｜**CDP／手動真登入在 `sys_token`／`session_event`（失敗另 `sys_login_attempt`）留下的 committed 殘列，配上測試守衛 `SequenceResetGuard::AUTH_SEQUENCES` 的 `setval(seq,1,false)` 復位，＝下一輪全量測試 nextval 落在殘列 id 上、23505 PK 衝突連環紅**：006-authz-governance final holistic（2026-08-24）實暴——U11 CDP 走查真登入留 sys_token 2 列＋session_event 2 列，走查後清理契約只清 casbin／archive／role／menu 面且 schema-gate 複驗綠（該三表在 RUNTIME_APPEND_TABLES 收窄集：剝列＋setval 值正規化＝結構性無感），帳面全綠；下一次全量（收刀閘）即 23 failed 全 duplicate key。兩個獨立盲點疊加：①「schema-gate 複驗綠」被誤當「環境已還原」——收窄集＝該閘的豁免名冊、不是無害證明（L-031③ 同理）；②守衛把 seq 復位到 1 而不驗表空、把殘列從「無害多餘」放大成「必紅」。防法：真登入類走查的清理契約必含三張 runtime-append 表 DELETE＋三 seq 復位（quickstart §4 已晉升）；守衛名冊本身的結構性修正另掛 B-121。同族第二形（同日坐實）：CDP 以 seed 角色為授權載體留下的 `sys_operation_log` 稽核列（合法 runtime 寫入、非 PK 衝突路徑）撞上 sys_role.rs 對 seed 角色的**絕對零稽核斷言**——runtime-append 表的殘列有兩條爆線：seq 復位撞 PK、絕對計數斷言撞列（B-122）。盲點＝殘列與爆點隔了一整輪（當輪全綠、下一輪才紅），時間差讓歸因直覺撲空。
