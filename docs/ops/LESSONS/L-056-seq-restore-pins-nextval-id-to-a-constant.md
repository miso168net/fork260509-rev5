---
promoted_to: rust-api `model/facade/sys_role.rs`／`sys_menu.rs` tests 之 `audit_rows_of`／`audit_rows_since` 型 doc（窗形判準就寫在兩支的分工句）＋`model/mod.rs` test_db 之 `SeedOpLogCleanup` 型 doc（同一取窗理由）
---
- **L-056**｜**清理守衛的 `setval` 還原，把 create 形測試「看似每輪都新」的 nextval id 釘成跨執行**定值**——於是 UI 走查建同型資料會取到同一個 id，其孤兒稽核列使絕對計數斷言誤紅**：維護批 A U1（2026-08-24）修 B-122 時，implementer 把兩支「鍵既非 seed 實體、也非測試自有顯式大 id」的斷言列為「拿不準、未改」升級主線（`sys_role.rs::create_lands_row_zero_casbin_with_same_txn_audit` 與 `sys_menu.rs::create_success_full_fields_oplog_zero_casbin_zero_reload` 的 `assert_eq!(audits.len(), 1)`），主線複核後判定該爆線為真並親修。★**機制**：`RoleCleanup`④ 的 `setval('sys_role_id_seq', 3, true)`／`MenuCleanup`③ 的 `setval('sys_menu_id_seq', 78, true)` 每輪 Drop 都把 seq 還原成 seed 值 ⇒ 下一輪 `create()` 走 nextval 拿到的**恆是同一個 id**（現況 sys_role=4、sys_menu=79）；而 CDP 走查以 UI 建一個角色／選單同樣從該 seq 取號、拿到**同一個 id**並連帶寫一列 `sys_operation_log`。走查後若清了業務列而沒清稽核列（`sys_operation_log` 在 `tools/schema-gate.py` 的 runtime-append 收窄集內、gate2 不對它逐列 diff ⇒ **零機器訊號**），該孤兒列就永久掛在那個 id 上，下一輪測試的 `audit_rows_of(created.id)` 撈到 2 列、絕對計數斷言 `left: 2, right: 1` 當場紅——爆點與成因隔了一整輪，與 L-055 兩形同族。★**實測坐實（非理論風險）**：主線種兩列孤兒（`entity_table='sys_role' entity_id=4`／`'sys_menu' entity_id=79`）後，watermark 形兩測皆綠；把斷言改回 `audit_rows_of` 絕對形，兩測當場紅（`sys_role.rs:1576`／`sys_menu.rs:3049`，訊息皆為「★同交易恰一列稽核」）；還原後再綠、孤兒列刪除後 op-log 歸零。★**防法（判準改寫）**：稽核列斷言要不要走 watermark 形，判準**不是**「鍵是不是 seed 實體 id」，而是**「這個 id，走查／真 runtime 造不造得出」**——①seed 實體 id（`sys_role` ≤3／`sys_menu` ≤78）造得出 ②**走 nextval 取得的新列 id 同樣造得出**（本條機制：被 seq 還原釘成定值）；只有**測試自有的顯式大 id 段**（9_3xx／9_4xx…）才是結構性造不出、可續用絕對形。★**連帶心得**：agent 回報裡的「拿不準、未改、升級主線」是**高價值訊號**——本條正是從那一句長出來的；主線收尾第①步（復核 agent 回報）遇到這類升級項時，成本通常只有兩行，但不追就等於把一個已知會爆的缺口留給下一刀（此處＝刀 B 的 CDP 走查必建 user／角色）。
