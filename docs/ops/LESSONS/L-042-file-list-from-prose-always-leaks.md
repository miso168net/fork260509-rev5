---
promoted_to: 未盤點（2026-08-17 分檔遷移前存量；盤點工項＝B-091 承載）
---
- **L-042**｜**執行單元的允許檔清單，從 task 文字建就一定會漏——因為 task 寫的是「要做什麼」，而清單要答的是「碰得到什麼」**：004 一刀內同型撞**三次**，且三次的徵狀完全一樣——implementer 做到一半發現**非動某個清單外檔不可**，而那個檔在 task 文字裡壓根沒被提到。①U-D：我要求把 `build_ruleset` 的告警移出純函式，卻沒把發射點指派給任何下游 task ②U-F：我寫「`login.rs` 不得動」，但 `LoginAttempt` 沒有 `Default`、加 `peer_ip` 就非動它的三個建構點不可 ③U-K：解鎖端點要落 `operation="unlock"` 的稽核列，而 `AuditOperation` 是**封閉列舉恰四變體**、`as_str` 是窮舉 match ⇒ **稽核列在型別上構造不出來**，`model/audit.rs` 卻不在清單。★**共同機制**：task 文字描述的是**行為**（「落一列稽核」），而允許清單要答的是**該行為會碰到哪些檔**——中間隔著「這個行為需要的值，現有型別容不容得下」這一層，而那一層只有讀碼才看得見。★**防法＝動工前的三問，逐項對實碼查（不是讀 task 文字）**：①**值域**——本單元要寫進去的每一個字面／列舉值，現有型別（enum／CHECK／NOT NULL／欄寬）容得下嗎？容不下就要動定義檔。②**建構點**——本單元要新增的結構欄位，其型別有 `Default` 嗎？沒有的話**所有既有建構點**都得動，逐一 grep 出來入清單。③**下游**——本單元產生的每一個新事實（新告警、新序列、新狀態），**有沒有任何一個 task 負責消費它**？沒有就是我漏派了。★**代價不對稱**：清單多列一個檔，agent 頂多不去動它；清單少列一個檔，就是整支 workflow 白跑（U-K 那次跑了 1 支 agent、22 分鐘就撞牆）。**寧可多列**。★**識別法**：受阻回報裡出現「在型別上構造不出來」「沒有 `Default`」「窮舉 match」「非動 X 不可而 X 不在清單」＝本形。

